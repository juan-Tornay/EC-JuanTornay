function calcularArea() {
  // Esto me recoje los numeros en sus cajas
  const ancho = parseFloat(document.getElementById('ancho').value);
  const alto = parseFloat(document.getElementById('alto').value);

  //Aqui declaro la operacion para crear la funcion y hago para que aparezca por pantalla
      if (1>0) {
          const area = ancho * alto; // Calcular el área
          document.getElementById('salida').textContent = `El área del rectángulo es: ${area}`; // Mostrar el resultado
      } 
    }
